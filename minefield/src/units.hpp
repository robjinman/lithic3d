#pragma once

#include <cstddef>
#include <cstdint>

const size_t TICKS_PER_SECOND = 60;

const size_t MAIN_SCISSOR = 1;
const size_t MOBILE_CONTROLS_SCISSOR = 2;

const uint32_t ATLAS_WIDTH_PX = 1024;
const uint32_t ATLAS_HEIGHT_PX = 512;
