* Hide world units from application developer (expose only metres)
* Improve shader manifest
  - Remove need to specify every feature flag (can be false by default)
* Split terrain cells into chunks
* Cascaded shadow maps
* Fix frustum culling
* Basic collision system