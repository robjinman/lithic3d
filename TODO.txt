* Particle emitter LODs
* Polyhedra collision volumes
* Polyhedra collision volumes in editor
* Aggregate collision volumes in editor
* Fix dynamic text rendering
* Underground areas
* Hide world units from application developer (expose only metres)
* Improve shader manifest
  - Remove need to specify every feature flag (can be false by default)
* Split terrain cells into chunks
* Devise a way for the application to declare its own library dependencies
* Consider use of Vulkan Profiles

PERFORMANCE
* Indirect draws - GPU frustum culling
* Compute mipmaps offline
* LOD mesh shading
* Use KTX instead of PNG for textures
